import 'package:flutter/material.dart';

class AnalystDashboard extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Analyst Dashboard")),
      body: Center(child: Text("Welcome Analyst")),
    );
  }
}
